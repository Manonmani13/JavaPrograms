package AccessModifierPack;

public class Synchornized {
//methods and blocks and 
	//for variables and classes->not applicable
	/*
	 * only one thread accessed at a time (data inconsitence) ->synchronized
	 * schrozied should contain implemenatation ->we cannot give abstract for synchronized
	 */
}
